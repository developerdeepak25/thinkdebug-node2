const NarrowLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="container mx-auto py-16 max-sm:px-4 px-12">{children}</div>
  );
};

export default NarrowLayout;
